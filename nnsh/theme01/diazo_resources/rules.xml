<?xml version="1.0" encoding="UTF-8"?>
<rules
    xmlns="http://namespaces.plone.org/diazo"
    xmlns:css="http://namespaces.plone.org/diazo/css"
    xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
    xmlns:xi="http://www.w3.org/2001/XInclude">

    <!-- Apply the following rules to standard Plone pages -->

    <rules if="$themeid = 'jango'">
        <xi:include href="jango.xml" />
    </rules>

    <rules if="$themeid = 'norma'">
        <xi:include href="norma.xml" />
    </rules>

    <rules if="$themeid = 'eross'">
        <xi:include href="eross.xml" />
    </rules>

    <rules if="$themeid = 'creatika'">
        <xi:include href="creatika.xml" />
    </rules>

    <rules if="$themeid = 'pearl'">
        <xi:include href="pearl.xml" />
    </rules>

    <rules if="$themeid = 'jollyness'">
        <xi:include href="jollyness.xml" />
    </rules>

    <rules if="$themeid = 'porto'">
        <xi:include href="porto.xml" />
    </rules>

    <rules if="$themeid = 'meteor'">
        <xi:include href="meteor.xml" />
    </rules>

</rules>
